package com.example.konstellation.constellationGenerator.dataClasses

enum class Subtype{
    APP,
    CHAT,
    ROOM,
    GROUP,
    CONSTELLATION
}

enum class MatrixSubType{
    APP,
    CHAT,
    ROOM
}
enum class MastodonSubType{
    APP,
    CHAT,
    GROUP
}
